import React from 'react';
import { Stack } from '@grafana/ui';

interface {{properCase templateName}}Props {

}

export const {{properCase templateName}} = (props: {{properCase templateName}}Props) => {

  return (
    <Stack grow={1} direction={'column'}></Stack>
  );
};

export default {{properCase templateName}};