---
title: RadioButtonGroup
description: Grafana Labs Radio Button component
keywords: ['Grafana', 'Labs', 'radio', 'button', 'group', 'component']
hide_title: true
---

import { Badge, Icon } from '@grafana/ui';

# RadioButtonGroup <Badge text='ready' color='green'></Badge> <a href='https://developers.grafana.com/ui/latest/index.html?path=/story/forms-radiobuttongroup--radio-buttons' target='_blank' className='header-links'>Storybook <Icon name="external-link-alt"/> </a>

RadioButtonGroup is used to select a single value from multiple mutually exclusive options using a horizontal tab-like layout.

<iframe
  src="https://developers.grafana.com/ui/latest/index.html?path=/story/forms-radiobuttongroup--radio-buttons&full=1&shortcuts=false&singleStory=true"
  width="100%"
  height="500px"
></iframe>

## When to use

Use `RadioButtonGroup` for mutually exclusive selections if there are up to four options available. This is because the `RadioButtonGroup` cannot have more than one row and should still accommodate small resolutions. For a mutually exclusive selection of more than four options, use `Select` component or the `RadioButtonList` component.

Radio buttons can only exist in this type of group. If you want one single option, it's better to use `Switch` instead. To offer multiple choices within the same group or context which are not mutually exclusive, use `Checkbox` instead.

## Usage

### Basic radio group

```jsx
<RadioButtonGroup
  options={[
    { label: 'Prometheus', value: 'prometheus' },
    { label: 'Graphite', value: 'graphite', icon: 'cloud' },
    { label: 'Elastic', value: 'elastic' },
  ]}
  value="prometheus"
  onChange={() => {}}
/>
```

### Disabled options

You can disable some options by passing them to the `disabledOptions` prop.
Keep in mind the `disabledOptions` are compared with options' values by the `===` operator.

```jsx
<RadioButtonGroup
  options={[
    { label: 'Prometheus', value: 'prometheus' },
    { label: 'Graphite', value: 'graphite', icon: 'cloud' },
    { label: 'Elastic', value: 'elastic' },
  ]}
  value="Graphite"
  disabledOptions={['prometheus', 'elastic']}
  onChange={() => {}}
/>
```

The whole control can be disabled with the `disabled` prop.

```jsx
<RadioButtonGroup
  disabled
  options={[
    { label: 'Prometheus', value: 'prometheus' },
    { label: 'Graphite', value: 'graphite', icon: 'cloud' },
    { label: 'Elastic', value: 'elastic' },
  ]}
  value="prometheus"
  onChange={() => {}}
/>
```

### Size

Specify the size `sm` for a smaller, more compact control. The small variant is commonly used inline in other elements, such as headers. Avoid using `sm` in forms.

```jsx
<RadioButtonGroup
  size="sm"
  options={[
    { label: 'Prometheus', value: 'prometheus' },
    { label: 'Graphite', value: 'graphite', icon: 'cloud' },
    { label: 'Elastic', value: 'elastic' },
  ]}
  value="prometheus"
  onChange={() => {}}
/>
```

### Width

By default `RadioButtonGroup` will fit its width to it's contents. Specify the `fullWidth` prop to make the control span the entire width of the container.

```jsx
<RadioButtonGroup
  fullWidth
  options={[
    { label: 'Prometheus', value: 'prometheus' },
    { label: 'Graphite', value: 'graphite', icon: 'cloud' },
    { label: 'Elastic', value: 'elastic' },
  ]}
  value="prometheus"
  onChange={() => {}}
/>
```
